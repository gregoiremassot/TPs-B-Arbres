#include <stdlib.h>
#include <stdio.h>

#include "barbre.h"
#include "general.h"
#include "barbre_bis.h"
#include "listes.h"

int main(int argc, char *argv[])
{
//    int *arbre;
//    arbre = malloc(10*sizeof(int));
    int arbre[17];
    printf("%d \n", sizeof(arbre)/sizeof(int));
    return 0;
}
